#ifndef ASSIGNMENT1_READLIST_H
#define ASSIGNMENT1_READLIST_H

int getInput(int argc, char file[]);
int specifiedFile(char file[]);
int searchDirectory();
int userInput();
int readContent(char arr[], int i, int *j);

#endif //ASSIGNMENT1_READLIST_H